import 'package:flutter/material.dart';

class GlobalAlertColors {
  GlobalAlertColors._();

  // ── Surface ────────────────────────────────────────────────
  static const card = Color(0xFFFFFFFF);
  static const border = Color(0xFFE8ECF0);
  static const secondaryBg = Color(0xFFF5F7FA);
  static const overlay = Color(0x55000000);

  // ── Text ───────────────────────────────────────────────────
  static const textPrimary = Color(0xFF1F2937);
  static const textSecondary = Color(0xFF6B7280);

  // ── Actions ────────────────────────────────────────────────
  static const primaryAction = Color(0xFF1E2D5B); // navy — matches app palette

  // ── Semantic icon colors ───────────────────────────────────
  static const successGreen = Color(0xFF22C55E);
  static const errorPink = Color(0xFFEF4444);
  static const warningAmber = Color(0xFFF59E0B);
  static const infoBlue = Color(0xFF3B82F6);
}
